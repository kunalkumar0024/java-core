package Arrays;

public class ArraysDemo {
    public static void main(String[] args) {
        String[] arr = {"Kuanl", "Manish", "Abhi"};
        System.out.println("Length of array is : " + arr.length);
        System.out.println("Elements of array are : " + arr[0] + ", " + arr[1] + ", " + arr[2]);
    }
}
